package server

import (
	"context"
)

type server struct {
    // ViVnfm interface IFA-006
    // OrVi interface IFA-005
}

func New() (*server, error) {
    return nil, nil
}


func (s *server) ListenAndServe(ctx context.Context, ip string) error {

    return nil
}

